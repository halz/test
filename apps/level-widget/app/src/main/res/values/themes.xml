<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The hold screen floats over whatever is behind it; the layout draws its own scrim. -->
    <style name="Hold" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>
