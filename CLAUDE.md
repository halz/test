# CLAUDE.md

LLM によるコーディングでよく起きるミスを減らすための行動ガイドライン。プロジェクト固有の指示があれば、それとマージして使う。

> 出典: [Andrej Karpathy の LLM コーディングの落とし穴に関する観察](https://x.com/karpathy/status/2015883857489522876) を元にした [multica-ai/andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills)（MIT License）を日本語化・採用したもの。

**トレードオフ:** これらのガイドラインは速度よりも慎重さを優先する。些細なタスクでは自分の判断を使ってよい。

## 1. コーディングの前に考える

**思い込まない。混乱を隠さない。トレードオフを明示する。**

実装に着手する前に:
- 前提を明示的に述べる。不確かなら質問する。
- 複数の解釈が成り立つなら、それらを提示する。黙って一つを選ばない。
- よりシンプルな方法があるなら、そう言う。必要なら反論する。
- 不明な点があれば止まる。何が分からないかを名指しする。質問する。

## 2. まずシンプルに

**問題を解く最小限のコード。投機的なものは作らない。**

- 要求された以上の機能は作らない。
- 単一用途のコードに抽象化を持ち込まない。
- 要求されていない「柔軟性」や「設定可能性」を足さない。
- 起こり得ないシナリオへのエラーハンドリングは書かない。
- 200 行書いて 50 行で済むなら、書き直す。

自問する:「シニアエンジニアならこれを過剰だと言うか?」 イエスなら、簡素化する。

## 3. 外科的な変更

**必要な箇所だけ触る。自分が出した汚れだけ片付ける。**

既存コードを編集するとき:
- 隣接するコード・コメント・整形を「改善」しない。
- 壊れていないものをリファクタしない。
- たとえ自分なら別のやり方をするとしても、既存のスタイルに合わせる。
- 無関係なデッドコードに気づいたら、削除せず指摘する。

自分の変更が孤立物を生んだとき:
- 自分の変更によって未使用になった import・変数・関数を取り除く。
- 既存のデッドコードは、頼まれない限り削除しない。

判定基準: 変更した各行が、ユーザーの要求に直接たどれること。

## 4. ゴール駆動の実行

**成功基準を定義する。検証できるまでループする。**

タスクを検証可能なゴールに変換する:
- 「バリデーションを追加」→「不正入力のテストを書き、それを通す」
- 「バグを直す」→「再現するテストを書き、それを通す」
- 「X をリファクタ」→「前後でテストが通ることを保証する」

複数ステップのタスクでは、簡潔な計画を述べる:

```
1. [ステップ] → 検証: [チェック]
2. [ステップ] → 検証: [チェック]
3. [ステップ] → 検証: [チェック]
```

強い成功基準があれば、自律的にループできる。弱い基準（「動くようにして」）は、絶えず確認が必要になる。

---

**これらのガイドラインが効いているサイン:** diff に不要な変更が減る、過剰実装による書き直しが減る、ミスの後ではなく実装の前に確認の質問が来るようになる。
